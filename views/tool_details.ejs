<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <title>
    <%= tool.title %> - Tool Sharing
  </title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.min.js"></script>
  <link href="/css/styles.css" rel="stylesheet">
</head>

<body class="container mt-5">
  <%- include('partials/header') %>
  <h1>
    <%= tool.title %>
  </h1>
  <p><strong>Description:</strong>
    <%= tool.description || 'No description' %>
  </p>
  <p><strong>Location:</strong>
    <%= tool.location || 'Not specified' %>
  </p>
  <% if (tool.user_manual_path) { %>
  <p><strong>Manual:</strong> <a href="/uploads/<%= tool.user_manual_path %>" target="_blank">Download
      Manual</a></p>
  <% } %>
  <h2>Images</h2>
  <div class="row">
    <% images.forEach(img=> { %>
    <div class="col-md-4 mb-3">
      <img src="/<%= img.image_path %>" class="img-fluid rounded" alt="Tool Image">
    </div>
    <% }); %>
  </div>
  <a href="/" class="btn btn-primary">Back to Search</a>
</body>

</html>